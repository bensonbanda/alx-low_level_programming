#include <stdio.h>

/**
 * main - entrly point
 * @void: zero parameter
 * Description: will print numbers to screen
 * Return: zero value
 */

int main(void)
{
	char numbers[] = "0123456789";

	puts(numbers);

	return (0);
}
