#include "main.h"

/*
 *_abs - will print absolute numbers
 *@v: interger parameter
 *Return: interger value
 */

int _abs(int v)
{
	if (v < 0)
	{
		v *= -1;

		return (v);
	}

	else
		return (v);
	return (0);
}
